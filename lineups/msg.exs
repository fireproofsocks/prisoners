alias Prisoners.Strategies.AlwaysCooperate

#Process.register(self(), :god)
#
#{:ok, pid} = AlwaysCooperate.start_link([])
#
#Process.exit(pid, :disqualified)
##Process.exit(pid, :kill)
#
##flush()
#receive do
#  {^pid, state} -> IO.inspect(state, label: "STATE")
##  _ -> nil
#end
